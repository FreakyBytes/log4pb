

Interface FormatParser
  setFormat( formatString.s )
  setMetaCharacters( openBracket.s, closeBracket.s, openQuoate.s, closeQuote.s, paramSeparator.s, escape.s, equal.s )
  parse.s( *message.log_msg )
EndInterface

; IDE Options = PureBasic 5.11 (Windows - x64)
; EnableXP
; EnableUser
; CompileSourceDirectory